{
  "name": "jarvis-websocket-router",
  "version": "1.0.0",
  "description": "Unified WebSocket Router for JARVIS AI Agent",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/websocket/server.js",
    "dev": "ts-node-dev --respawn --transpile-only server.ts",
    "watch": "tsc -w",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "ws": "^8.14.2",
    "dotenv": "^16.3.1",
    "url": "^0.11.3"
  },
  "devDependencies": {
    "@types/node": "^20.8.0",
    "@types/ws": "^8.5.8",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}