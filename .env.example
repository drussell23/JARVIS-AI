# JARVIS AI Agent Environment Variables
# Copy this file to .env and fill in your values

# ============================================================================
# CLOUD SQL DATABASE CONFIGURATION
# ============================================================================
# Database type: "cloudsql" for Google Cloud SQL, "sqlite" for local database
JARVIS_DB_TYPE=cloudsql

# Cloud SQL connection name (format: project:region:instance)
JARVIS_DB_CONNECTION_NAME=your-project:region:instance-name

# Database host (127.0.0.1 for Cloud SQL proxy, or Cloud SQL private IP)
JARVIS_DB_HOST=127.0.0.1

# Database port (default: 5432 for PostgreSQL)
JARVIS_DB_PORT=5432

# Database password
JARVIS_DB_PASSWORD=your-database-password

# ============================================================================
# GOOGLE CLOUD PLATFORM (GCP) CONFIGURATION
# ============================================================================
# Path to GCP service account credentials JSON file
GOOGLE_APPLICATION_CREDENTIALS=/path/to/your/service-account-key.json

# GCP project ID
GCP_PROJECT_ID=your-gcp-project-id

# Enable/disable GCP Spot VM auto-creation (true/false)
GCP_VM_ENABLED=true

# ============================================================================
# ANTHROPIC CLAUDE API
# ============================================================================
# Anthropic API key for Claude
ANTHROPIC_API_KEY=your-anthropic-api-key

# ============================================================================
# VOICE SERVICES
# ============================================================================
# Picovoice access key for wake word detection
PICOVOICE_ACCESS_KEY=your-picovoice-access-key

# ============================================================================
# THIRD-PARTY API KEYS (Optional Services)
# ============================================================================
# ElevenLabs API key for text-to-speech (premium voice synthesis)
ELEVENLABS_API_KEY=your-elevenlabs-api-key

# OpenWeather API key for weather data
OPENWEATHER_API_KEY=your-openweather-api-key

# ============================================================================
# DATABASE CONFIGURATION (Additional Settings)
# ============================================================================
# Database name (default: jarvis)
JARVIS_DB_NAME=jarvis

# Database user (default: postgres)
JARVIS_DB_USER=postgres

# ============================================================================
# BACKEND SERVER CONFIGURATION
# ============================================================================
# Backend server port (default: 8000)
BACKEND_PORT=8000

# WebSocket router port (default: 8765)
WS_ROUTER_PORT=8765

# CORS allowed origins (comma-separated, default: http://localhost:3000)
CORS_ORIGINS=http://localhost:3000

# ============================================================================
# JARVIS USER & VOICE CONFIGURATION
# ============================================================================
# Your username for personalized responses
JARVIS_USER=your-name

# Enable/disable voice features (true/false)
JARVIS_VOICE_ENABLED=true

# Enable/disable web UI (true/false)
JARVIS_WEB_UI=true

# ============================================================================
# PERFORMANCE & OPTIMIZATION
# ============================================================================
# Enable optimized startup (reduces initialization time)
OPTIMIZE_STARTUP=true

# Enable parallel component loading (faster startup)
USE_PARALLEL_STARTUP=true

# Memory-optimized mode for lower RAM usage (true/false)
MEMORY_OPTIMIZED_MODE=false

# Backend parallel imports for faster startup
BACKEND_PARALLEL_IMPORTS=true

# Lazy load ML models to reduce memory usage
BACKEND_LAZY_LOAD_MODELS=true

# ============================================================================
# VISION & SCREEN ANALYSIS
# ============================================================================
# Enable continuous vision monitoring
VISION_CONTINUOUS_ENABLED=false

# Vision cache size in MB (default: 100)
VISION_CACHE_SIZE_MB=100

# Maximum concurrent vision requests (default: 3)
VISION_MAX_CONCURRENT=3

# Vision analysis timeout in seconds (default: 30)
VISION_TIMEOUT=30

# ============================================================================
# WAKE WORD DETECTION
# ============================================================================
# Wake word detection sensitivity (0.0-1.0, higher = more sensitive)
WAKE_WORD_THRESHOLD=0.5

# Enable wake word detection
WAKE_WORD_ENABLED=true

# ============================================================================
# ADVANCED FEATURES (Optional)
# ============================================================================
# Enable goal automation and inference
JARVIS_GOAL_AUTOMATION=false

# Quadtree spatial optimization for vision analysis
QUADTREE_SPATIAL_ENABLED=false

# Enable Picovoice wake word engine (requires PICOVOICE_ACCESS_KEY)
USE_PICOVOICE=true

# Enable quantized ML models for faster/lighter inference
USE_QUANTIZED_MODELS=false

# Enable TensorFlow backend
USE_TF=false

# Enable PyTorch backend
USE_TORCH=false

# Enable integration orchestrator for multi-component coordination
INTEGRATION_ORCHESTRATOR_ENABLED=false

# Enable vision memory-efficient mode
VISION_MEMORY_EFFICIENT_ENABLED=true

# Enable Swift vision coordinator (macOS only)
VISION_SWIFT_ENABLED=false

# Vision model selection (claude-3-sonnet, claude-3-opus, etc.)
VISION_MODEL=claude-3-sonnet-20240229

# Maximum vision API tokens per request
VISION_MAX_TOKENS=1024

# WebSocket endpoint for real-time communication
WEBSOCKET_ENDPOINT=ws://localhost:8765

# Enable solution memory for problem-solving context
SOLUTION_MEMORY_ENABLED=true

# Minimum confidence for auto-applying solutions (0.0-1.0)
SOLUTION_MIN_CONFIDENCE=0.8

# Solution storage path
SOLUTION_STORAGE_PATH=~/.jarvis/solutions

# Auto-approve space switching (for multi-desktop workflows)
JARVIS_AUTO_APPROVE_SPACE_SWITCH=false

# Enable task inference for proactive assistance
TASK_INFERENCE=false

# Monitored desktop spaces (comma-separated numbers)
MONITORED_SPACES=1,2,3

# Monitor only active desktop for performance
MONITOR_ACTIVE_ONLY_PRESSURE=true

# ============================================================================
# PROACTIVE FEATURES
# ============================================================================
# Enable proactive monitoring and suggestions
JARVIS_PROACTIVE=false

# Proactive analysis interval in seconds
PROACTIVE_ANALYSIS_INTERVAL=60

# Minimum confidence for proactive notifications (0.0-1.0)
PROACTIVE_CONFIDENCE_THRESHOLD=0.7

# Enable proactive voice announcements
PROACTIVE_VOICE_ENABLED=false

# Enable vision proactive analysis
VISION_PROACTIVE_ENABLED=false

# ============================================================================
# MEMORY & CACHING
# ============================================================================
# Memory optimization level (low/medium/high)
JARVIS_MEMORY_LEVEL=medium

# Maximum memory usage percentage (0-100)
JARVIS_MAX_MEMORY_PERCENT=80

# Cache TTL in seconds
JARVIS_CACHE_TTL=3600

# Enable context-aware caching
ENABLE_CONTEXT_CACHING=true

# Context cache size (number of items)
CONTEXT_CACHE_SIZE=100

# ============================================================================
# GCP CLOUD CONFIGURATION (Additional Settings)
# ============================================================================
# GCP region (default: us-central1)
GCP_REGION=us-central1

# GCP zone (default: us-central1-a)
GCP_ZONE=us-central1-a

# ============================================================================
# OPTIONAL CONFIGURATION
# ============================================================================
# Environment (development/production)
JARVIS_ENV=production

# Log level (DEBUG/INFO/WARNING/ERROR/CRITICAL)
LOG_LEVEL=INFO

# Debug mode - verbose logging (true/false)
JARVIS_DEBUG=false
