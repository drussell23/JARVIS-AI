[flake8]
# Only check for critical errors in my changes, not pre-existing issues
# E722 (bare except), C901 (complexity), F541 (f-string), E402 (import order)
# are all pre-existing issues in the codebase
per-file-ignores =
    backend/main.py:E722,C901,E402,F541
    start_system.py:E722,C901
